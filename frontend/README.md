# frontend

Placeholder — the Next.js app hasn't been scaffolded here yet.

Run this from the repo root to generate it:

```bash
npx create-next-app@latest frontend --typescript --tailwind --eslint
```

See [`../SETUP.md`](../SETUP.md) for the full setup sequence, including the extra dependencies to install afterward (`@clerk/nextjs`, `lottie-react`).
